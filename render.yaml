services:
  - type: web
    name: cargo-game
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn MVP.server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
